# CLI Command History

## `ct dump 20-`

The command "ct dump 20-" is not a standard command, but it appears to be an incomplete or malformed version of the "ct dump" command, which is likely used to dump or display information.

## `ct dump 20`

The command "ct dump 20" dumps or displays information for the specified entity, likely a test case or suite, with the number 20.

## `ct dump 40`

The command "ct dump 40" dumps or displays information for the specified entity, likely a test case or suite, with the number 40.

## `ct dump 60`

The command "ct dump 60" dumps or displays information for the specified entity, likely a test case or suite, with the number 60.

## `make build`

The command "make build" is used to build a project or application using the make build system, compiling and linking source code into an executable.

## `ct dump 5`

The command "ct dump 5" dumps or displays information for the specified entity, likely a test case or suite, with the number 5.

