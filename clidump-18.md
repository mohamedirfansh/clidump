# CLI Command History

## `ct dump 20-`

The command "ct dump 20-" is not a standard or recognizable command, and its explanation depends on the specific context or system in which it is being used.

## `ct dump 20`

The command "ct dump 20" likely dumps or displays the contents of a specific test case or suite, with "20" possibly referring to a test case number or identifier.

## `ct dump 40`

The command "ct dump 40" is similar to the previous one, but with "40" as the test case number or identifier being dumped or displayed.

## `ct dump 60`

The command "ct dump 60" follows the same pattern, dumping or displaying the contents of a test case or suite with the identifier "60".

## `make build`

The command "make build" is used to build or compile a project using the make build system, which automates the process of compiling and linking source code.

## `ct dump 5`

The command "ct dump 5" dumps or displays the contents of a test case or suite with the identifier "5", following the same pattern as the previous "ct dump" commands.

