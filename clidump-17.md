# CLI Command History

## `ct dump 20-`

The command "ct dump 20-" is not a standard command and its explanation cannot be provided without context, but it appears to be a variation of the ct dump command with an invalid or incomplete parameter.

## `ct dump 20`

The command "ct dump 20" dumps the contents of a capture or log, likely showing the last 20 entries or items.

## `ct dump 40`

The command "ct dump 40" dumps the contents of a capture or log, showing the last 40 entries or items, similar to the previous command but with a larger output.

## `ct dump 60`

The command "ct dump 60" dumps the contents of a capture or log, displaying the last 60 entries or items, providing an even larger output than the previous commands.

## `make build`

The command "make build" is used to build or compile a project, using a Makefile that contains the necessary instructions and dependencies for the build process.

## `ct dump 5`

The command "ct dump 5" dumps the contents of a capture or log, showing the last 5 entries or items, which is a smaller output compared to the other ct dump commands listed.

